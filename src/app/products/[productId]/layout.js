export default function ProductDetailsLayout({ children }) {
    return (
        <>
            {children}
            <h2>
                Nested Layout - Features products
            </h2>
        </>
    )
}