import Card from '@/components/card';

export default function DefaultRevenueMetrics () {
    // Default view of revenue slot of ComplexDashboard on page reload
    return (
        <Card>Revenue Metrics</Card>
    )
}